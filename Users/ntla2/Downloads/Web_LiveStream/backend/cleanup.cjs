const { PrismaClient } = require('@prisma/client'); const prisma = new PrismaClient(); async function clean() { const orders = await prisma.shipment.findMany({ orderBy: { createdAt: 'asc' } }); const seen = new Set(); for (const o of orders) { const key = o.phone + '_' + o.giftName; if (seen.has(key)) { await prisma.shipment.delete({ where: { id: o.id } }); console.log('Deleted duplicate:', o.id); } else { seen.add(key); } } console.log('Cleanup done.'); } clean().finally(() => prisma.$disconnect());
